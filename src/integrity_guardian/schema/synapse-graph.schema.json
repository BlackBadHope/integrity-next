{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://integrity.guardian/schema/v1/synapse-graph.schema.json",
  "title": "Integrity Synapse Graph v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "protocol",
    "graph_id",
    "tenant_id",
    "nodes",
    "edges",
    "capabilities"
  ],
  "properties": {
    "protocol": { "const": "integrity-guardian/synapse-graph/v1" },
    "graph_id": {
      "type": "string",
      "pattern": "^sha256:[a-f0-9]{64}$"
    },
    "tenant_id": { "$ref": "#/$defs/tenant_id" },
    "nodes": {
      "type": "array",
      "maxItems": 10000,
      "items": { "$ref": "#/$defs/node" }
    },
    "edges": {
      "type": "array",
      "maxItems": 25000,
      "items": { "$ref": "#/$defs/edge" }
    },
    "capabilities": { "$ref": "#/$defs/offline_capabilities" }
  },
  "$defs": {
    "id": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._:/-]{0,255}$"
    },
    "tenant_id": {
      "type": "string",
      "pattern": "^tenant:[A-Za-z0-9][A-Za-z0-9._:/-]{0,248}$"
    },
    "digest": {
      "type": "string",
      "pattern": "^sha256:[a-f0-9]{64}$"
    },
    "node_id": {
      "type": "string",
      "pattern": "^synapse-node:[a-f0-9]{64}$"
    },
    "edge_id": {
      "type": "string",
      "pattern": "^synapse-edge:[a-f0-9]{64}$"
    },
    "primitive": {
      "oneOf": [
        { "type": "string", "maxLength": 4096 },
        { "type": "integer" },
        { "type": "boolean" },
        { "type": "null" }
      ]
    },
    "node": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "node_id",
        "kind",
        "ref",
        "label",
        "truth_status",
        "attributes"
      ],
      "properties": {
        "node_id": { "$ref": "#/$defs/node_id" },
        "kind": {
          "enum": [
            "current_state",
            "intent",
            "evidence",
            "artifact",
            "decision",
            "action",
            "verification",
            "rollback"
          ]
        },
        "ref": { "type": "string", "minLength": 1, "maxLength": 4096 },
        "label": { "type": "string", "minLength": 1, "maxLength": 4096 },
        "truth_status": {
          "enum": [
            "reported",
            "observed",
            "verified",
            "derived",
            "proposed",
            "unknown",
            "stale"
          ]
        },
        "attributes": {
          "type": "object",
          "additionalProperties": { "$ref": "#/$defs/primitive" }
        }
      }
    },
    "condition": {
      "type": "object",
      "additionalProperties": false,
      "required": ["fact", "operator", "value"],
      "properties": {
        "fact": { "$ref": "#/$defs/id" },
        "operator": {
          "enum": ["equals", "not_equals", "exists", "absent"]
        },
        "value": { "$ref": "#/$defs/primitive" }
      }
    },
    "provenance": {
      "type": "object",
      "additionalProperties": false,
      "required": ["class", "source_id", "source_digest", "explanation"],
      "properties": {
        "class": {
          "enum": ["DECLARED", "OBSERVED", "DERIVED", "LEARNED", "PROPOSED"]
        },
        "source_id": { "$ref": "#/$defs/id" },
        "source_digest": { "$ref": "#/$defs/digest" },
        "explanation": {
          "type": "string",
          "minLength": 1,
          "maxLength": 4096
        }
      }
    },
    "edge": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "edge_id",
        "from_node_id",
        "to_node_id",
        "capability_id",
        "subsystem",
        "transition_kind",
        "authority",
        "evidence_requirements",
        "preconditions",
        "postconditions",
        "verifier",
        "reversibility",
        "blast_radius",
        "cost",
        "provenance"
      ],
      "properties": {
        "edge_id": { "$ref": "#/$defs/edge_id" },
        "from_node_id": { "$ref": "#/$defs/node_id" },
        "to_node_id": { "$ref": "#/$defs/node_id" },
        "capability_id": { "$ref": "#/$defs/id" },
        "subsystem": { "$ref": "#/$defs/id" },
        "transition_kind": {
          "enum": [
            "observe",
            "analyze",
            "plan",
            "disposable_mutation",
            "production_mutation",
            "verify",
            "rollback"
          ]
        },
        "authority": {
          "type": "object",
          "additionalProperties": false,
          "required": ["required_authority_ids", "production_authority"],
          "properties": {
            "required_authority_ids": {
              "type": "array",
              "uniqueItems": true,
              "items": { "$ref": "#/$defs/id" }
            },
            "production_authority": { "type": "boolean" }
          }
        },
        "evidence_requirements": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["evidence_id", "max_age_seconds"],
            "properties": {
              "evidence_id": { "$ref": "#/$defs/id" },
              "max_age_seconds": {
                "type": "integer",
                "minimum": 0,
                "maximum": 31536000
              }
            }
          }
        },
        "preconditions": {
          "type": "array",
          "uniqueItems": true,
          "items": { "$ref": "#/$defs/condition" }
        },
        "postconditions": {
          "type": "array",
          "uniqueItems": true,
          "items": { "$ref": "#/$defs/condition" }
        },
        "verifier": {
          "type": "object",
          "additionalProperties": false,
          "required": ["verifier_id", "required"],
          "properties": {
            "verifier_id": {
              "oneOf": [
                { "$ref": "#/$defs/id" },
                { "type": "null" }
              ]
            },
            "required": { "type": "boolean" }
          }
        },
        "reversibility": {
          "type": "object",
          "additionalProperties": false,
          "required": ["mode", "rollback_edge_id"],
          "properties": {
            "mode": {
              "enum": ["not_applicable", "automatic", "manual", "irreversible"]
            },
            "rollback_edge_id": {
              "oneOf": [
                { "$ref": "#/$defs/edge_id" },
                { "type": "null" }
              ]
            }
          }
        },
        "blast_radius": {
          "enum": ["none", "process", "host", "tenant", "network"]
        },
        "cost": {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "latency_ms",
            "context_bytes",
            "model_calls",
            "tool_calls",
            "resource_units"
          ],
          "properties": {
            "latency_ms": { "type": "integer", "minimum": 0 },
            "context_bytes": { "type": "integer", "minimum": 0 },
            "model_calls": { "type": "integer", "minimum": 0 },
            "tool_calls": { "type": "integer", "minimum": 0 },
            "resource_units": { "type": "integer", "minimum": 0 }
          }
        },
        "provenance": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": { "$ref": "#/$defs/provenance" }
        }
      }
    },
    "offline_capabilities": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "credentials",
        "execution",
        "model_sdk",
        "network",
        "production_authority"
      ],
      "properties": {
        "credentials": { "const": false },
        "execution": { "const": false },
        "model_sdk": { "const": false },
        "network": { "const": false },
        "production_authority": { "const": false }
      }
    }
  }
}
